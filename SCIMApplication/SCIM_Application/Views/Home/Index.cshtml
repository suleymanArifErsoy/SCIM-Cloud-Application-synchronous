@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="text-center">
    <h1 class="display-4">SCIM Yönetim Sistemi</h1>
    <p>Kullanıcı ekleme sayfasına yönlendiriliyorsunuz...</p>
    <div class="mt-3">
        <a href="@Url.Action("Create", "User")" class="btn btn-primary">Kullanıcı Ekle</a>
        <a href="@Url.Action("Index", "User")" class="btn btn-secondary ms-2">Kullanıcıları Görüntüle</a>
    </div>
</div>
